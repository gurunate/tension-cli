#!/usr/bin/env node

const pkg = require('../package');

require('commander')
    .version(pkg.version)
    .command('init', 'generate a project from a template')
    .command('ls', 'list available templates')
    .command('add', 'add template repository')
    .command('rm', 'remove template repository')
    .parse(process.argv);
